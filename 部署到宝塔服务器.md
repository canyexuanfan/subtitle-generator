# 🚀 部署到宝塔服务器 - 完整流程

## 📦 第一步：准备部署文件

### 需要上传的文件（共3个）
```
✅ index.html        # 主页面
✅ script.js         # JavaScript逻辑（已修复字幕背景问题）
✅ style.css         # 样式文件
```

### 不需要上传的文件
```
❌ server.py                    # 仅本地开发用
❌ prd.md                       # 文档
❌ *.md                         # 所有markdown文档
❌ image.png                    # 项目图片（如果页面不需要）
❌ subtitle-image-*.jpg/png     # 示例图片
❌ nginx-config-*.conf          # 配置文件（后面手动配置）
```

---

## 🌐 第二步：登录宝塔面板

```
访问: http://你的服务器IP:8888
域名: subtitle.hnwen17.top
```

---

## 📁 第三步：上传文件到服务器

### 方法A：宝塔面板在线上传（推荐）

1. **进入文件管理**
   - 点击左侧 **"文件"** 菜单
   - 导航到 `/www/wwwroot/subtitle-generator/`

2. **清空旧文件（如果有）**
   - 选中所有旧文件
   - 点击 **"删除"**

3. **上传新文件**
   - 点击 **"上传"** 按钮
   - 选择以下3个文件：
     - `index.html`
     - `script.js` （✅ 已修复版本）
     - `style.css`
   - 等待上传完成

4. **验证文件**
   ```bash
   # 应该看到这3个文件
   index.html
   script.js
   style.css
   ```

### 方法B：使用SSH命令（可选）

```bash
# 1. 连接服务器
ssh root@你的服务器IP

# 2. 进入网站目录
cd /www/wwwroot/subtitle-generator/

# 3. 清空目录
rm -rf *

# 4. 然后使用FTP工具上传文件
```

---

## ⚙️ 第四步：配置Nginx

### 1. 打开配置文件

1. 点击左侧 **"网站"** 菜单
2. 找到 `subtitle.hnwen17.top`
3. 点击 **"设置"**
4. 点击 **"配置文件"**

### 2. 替换配置内容

**完整配置如下**（复制粘贴替换）：

```nginx
server
{
    listen 80;
    listen [::]:80;
    server_name subtitle.hnwen17.top;

    # 静态站点配置
    index index.html index.htm;
    root /www/wwwroot/subtitle-generator;

    # 字符编码
    charset utf-8;

    # 文件上传大小限制（用户上传图片最大10MB）
    client_max_body_size 10m;
    client_body_buffer_size 128k;

    #CERT-APPLY-CHECK--START
    # 用于SSL证书申请时的文件验证相关配置 -- 请勿删除
    include /www/server/panel/vhost/nginx/well-known/subtitle.hnwen17.top.conf;
    #CERT-APPLY-CHECK--END

    include /www/server/panel/vhost/nginx/extension/subtitle.hnwen17.top/*.conf;

    #SSL-START SSL相关配置，请勿删除或修改下一行带注释的404规则
    #error_page 404/404.html;
    #SSL-END

    # ============================================
    # 安全响应头配置
    # ============================================

    # 防止点击劫持攻击
    add_header X-Frame-Options "SAMEORIGIN" always;

    # 防止MIME类型嗅探
    add_header X-Content-Type-Options "nosniff" always;

    # 启用XSS过滤器
    add_header X-XSS-Protection "1; mode=block" always;

    # 引荐来源策略
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # 权限策略
    add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;

    # Content Security Policy
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self'; connect-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self';" always;

    # ============================================
    # Gzip压缩配置
    # ============================================

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/rss+xml
        font/truetype
        font/opentype
        application/vnd.ms-fontobject
        image/svg+xml;
    gzip_disable "msie6";

    # ============================================
    # 错误页面配置
    # ============================================

    error_page 404 /404.html;
    error_page 500 502 503 504 /50x.html;

    # ============================================
    # 静态文件缓存策略
    # ============================================

    # 图片文件 - 缓存7天
    location ~* \.(gif|jpg|jpeg|png|bmp|ico|webp|svg)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
        access_log off;
        error_log off;
    }

    # CSS和JavaScript文件 - 缓存7天
    location ~* \.(js|css)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
        access_log off;
        error_log off;
    }

    # 字体文件 - 缓存30天
    location ~* \.(ttf|ttc|otf|eot|woff|woff2)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
        error_log off;
    }

    # HTML文件 - 不缓存
    location ~* \.(html|htm)$ {
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # ============================================
    # 安全性配置
    # ============================================

    # 禁止访问隐藏文件和敏感文件
    location ~ ^/(\.user\.ini|\.htaccess|\.git|\.env|\.svn|\.project|LICENSE|README\.md|package\.json|\.gitignore)$ {
        deny all;
        return 404;
    }

    # 禁止访问备份文件
    location ~* \.(bak|backup|swp|tmp|log|sql|gz|zip|rar)$ {
        deny all;
        return 404;
    }

    # ============================================
    # SSL证书验证
    # ============================================

    location ~ \.well-known {
        allow all;
    }

    if ( $uri ~ "^/\.well-known/.*\.(php|jsp|py|js|css|lua|ts|go|zip|tar\.gz|rar|7z|sql|bak)$" ) {
        return 403;
    }

    # ============================================
    # 主要路由配置
    # ============================================

    location / {
        try_files $uri $uri/ /index.html;
        autoindex off;
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    #REWRITE-START URL重写规则引用
    include /www/server/panel/vhost/rewrite/subtitle.hnwen17.top.conf;
    #REWRITE-END

    # ============================================
    # 日志配置
    # ============================================

    access_log  /www/wwwlogs/subtitle.hnwen17.top.log;
    error_log   /www/wwwlogs/subtitle.hnwen17.top.error.log;
}
```

### 3. 保存并重载

1. 点击 **"保存"** 按钮
2. 点击 **"重载配置"** 或直接重启Nginx

---

## 🔒 第五步：配置SSL证书（强烈推荐）

### 1. 申请Let's Encrypt免费证书

1. 在网站设置中，点击 **"SSL"** 标签
2. 选择 **"Let's Encrypt"**
3. 勾选域名 `subtitle.hnwen17.top`
4. 点击 **"申请"**
5. 等待申请成功（通常1-2分钟）

### 2. 开启强制HTTPS

1. 申请成功后，找到 **"强制HTTPS"** 选项
2. 开启开关 ✅
3. 保存设置

### 3. 更新Nginx配置（HTTPS版本）

如果需要更详细的HTTPS配置，可以参考项目中的 `nginx-config-https.conf` 文件。

---

## ✅ 第六步：测试验证

### 1. 访问网站

```
HTTP:  http://subtitle.hnwen17.top
HTTPS: https://subtitle.hnwen17.top
```

### 2. 功能测试清单

- [ ] **页面加载**
  - [ ] 页面正常打开
  - [ ] CSS样式正确
  - [ ] 无JavaScript错误（F12查看Console）

- [ ] **图片上传**
  - [ ] 可以点击上传图片
  - [ ] 可以拖拽上传图片
  - [ ] 图片预览正常

- [ ] **字幕功能**
  - [ ] 可以输入字幕文字
  - [ ] 调整字体大小、颜色等设置生效
  - [ ] 实时预览功能正常

- [ ] **字幕背景测试（重要！）**
  - [ ] 输入3-5行字幕
  - [ ] **所有字幕行背景应该一致**
  - [ ] 第2、3行背景 = 第1行背景（原图底部）

- [ ] **生成和下载**
  - [ ] 点击"生成字幕图片"功能正常
  - [ ] 点击"保存图片"可以下载
  - [ ] 下载的图片质量正常

### 3. 性能检查

打开浏览器开发者工具（F12）：

1. **Network标签**
   - 所有资源加载成功（绿色200状态）
   - 页面加载时间 < 2秒

2. **Console标签**
   - 无红色错误信息
   - 无警告信息（黄色可忽略）

3. **Lighthouse测试**（可选）
   ```
   F12 → Lighthouse → Generate Report
   目标分数：
   - Performance: > 85
   - Accessibility: > 90
   - Best Practices: > 90
   ```

---

## 🐛 常见问题排查

### 问题1: 访问404错误

**原因**: 文件未正确上传或路径错误

**解决**:
```bash
# SSH登录服务器检查
cd /www/wwwroot/subtitle-generator/
ls -la

# 应该看到
index.html
script.js
style.css
```

### 问题2: 样式不生效

**原因**: CSS文件路径错误或缓存问题

**解决**:
1. 清除浏览器缓存：`Ctrl + Shift + Delete`
2. 强制刷新：`Ctrl + F5`
3. 检查 `index.html` 中的CSS引用：
   ```html
   <link rel="stylesheet" href="style.css">  ✅ 正确
   ```

### 问题3: 字幕背景不一致

**原因**: 浏览器缓存了旧版本的 `script.js`

**解决**:
1. **强制刷新**: `Ctrl + Shift + R`
2. **清除缓存**:
   - Chrome: `Ctrl + Shift + Delete`
   - 选择"缓存的图片和文件"
   - 清除数据
3. **验证文件版本**:
   - F12 → Sources 标签
   - 查看 `script.js` 文件
   - 搜索 `const scale =` （应该在318行左右）
   - 确认有这行代码

### 问题4: 上传图片失败

**原因**: 文件大小超过Nginx限制

**解决**:
确认Nginx配置中有：
```nginx
client_max_body_size 10m;
```

### 问题5: HTTPS证书申请失败

**原因**: 域名未正确解析或80端口未开放

**解决**:
1. **检查域名解析**:
   ```bash
   ping subtitle.hnwen17.top
   # 应该解析到服务器IP
   ```

2. **检查防火墙**:
   - 宝塔面板 → 安全
   - 确保开放 80 和 443 端口

3. **重试申请**:
   - 等待5分钟后重试
   - 或使用其他SSL证书服务商

---

## 📊 部署验收清单

### 必须通过的测试

- [ ] ✅ 网站可以正常访问
- [ ] ✅ HTTPS已启用并强制跳转
- [ ] ✅ 所有页面资源加载成功
- [ ] ✅ 图片上传功能正常
- [ ] ✅ 字幕设置功能正常
- [ ] ✅ **字幕背景显示一致**（重点！）
- [ ] ✅ 生成和下载功能正常
- [ ] ✅ 浏览器Console无错误
- [ ] ✅ 移动端访问正常

### 性能指标

- [ ] ✅ 首次加载时间 < 3秒
- [ ] ✅ Lighthouse Performance > 85
- [ ] ✅ 上传10MB图片响应 < 2秒

---

## 🎉 部署完成后

### 1. 备份配置

在宝塔面板：
- **计划任务** → **添加任务**
- 任务类型: 备份网站
- 执行周期: 每周一次
- 备份到: 服务器本地 + 云存储（如有）

### 2. 监控设置

- **日志查看**: `/www/wwwlogs/subtitle.hnwen17.top.log`
- **错误日志**: `/www/wwwlogs/subtitle.hnwen17.top.error.log`
- 定期检查日志文件大小，避免占用过多空间

### 3. 后续优化

参考项目中的 `代码优化建议.md`：
- 添加更多安全加固措施
- 实施性能优化
- 考虑CDN加速（可选）

---

## 📞 需要帮助？

如果部署过程中遇到问题：

1. **查看日志**:
   ```bash
   tail -f /www/wwwlogs/subtitle.hnwen17.top.error.log
   ```

2. **重启Nginx**:
   - 宝塔面板 → 软件商店 → Nginx → 重启

3. **检查文件权限**:
   ```bash
   cd /www/wwwroot/subtitle-generator/
   chmod 644 *.html *.js *.css
   ```

---

**部署指南版本**: v2.0
**最后更新**: 2026-01-05
**修复内容**: 字幕背景坐标转换问题
**测试状态**: ✅ 本地测试通过
