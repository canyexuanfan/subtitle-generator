# 字幕背景修复 - 测试指南

## 🎯 测试目标
验证所有字幕行的背景都使用原图底部内容（而不是从不同位置截取）

---

## ✅ 测试步骤

### 1. 启动本地服务器
```bash
# Windows
python server.py

# 或指定端口
python server.py 8080
```

### 2. 打开网页
访问: `http://localhost:8000`

### 3. 上传测试图片
选择一张**底部有明显特征**的图片，例如：
- 底部有独特的颜色
- 底部有文字或图案
- 底部有渐变效果

### 4. 添加多行字幕
在字幕内容框中输入3-5行文字：
```
这是第一行字幕
这是第二行字幕
这是第三行字幕
这是第四行字幕
```

### 5. 观察字幕背景

#### ✅ 修复后的正确效果：
- 所有字幕行的背景应该**看起来一致**
- 所有字幕行都应该显示**原图底部的内容**
- 背景图案/颜色/纹理应该**完全相同**

#### ❌ 修复前的错误效果：
- 第2行、第3行等字幕背景会显示原图中间或上方的内容
- 背景图案不一致，每行不同

### 6. 生成并下载图片
1. 点击"生成字幕图片"
2. 点击"保存图片"
3. 检查下载的图片，确认字幕背景一致

---

## 🖼️ 推荐测试图片

### 理想的测试图片特征：
1. **渐变背景** - 从上到下颜色变化明显（如日落、海洋）
2. **底部有文字** - 容易识别是否使用了底部内容
3. **分层图片** - 底部、中部、顶部颜色差异大

### 示例场景：
- 📸 日落/日出照片（天空颜色渐变）
- 🌊 海滩照片（沙滩在底部）
- 🏙️ 城市天际线（建筑在底部）
- 🌄 风景照片（地面在底部）

---

## 🔍 核心验证点

### ✅ 通过测试的标准：
- [ ] 第1行字幕背景 = 原图底部
- [ ] 第2行字幕背景 = 原图底部（不是中间）
- [ ] 第3行字幕背景 = 原图底部（不是上方）
- [ ] 所有字幕行背景视觉上一致

### ❌ 未通过的标志：
- [ ] 不同字幕行背景颜色/图案不同
- [ ] 能看出背景是从原图不同位置截取的

---

## 🐛 如果测试失败

### 检查项：
1. **确认修改已保存**: 检查 `script.js:333` 行
   ```javascript
   sourceY = originalHeight - lineHeight; // 应该是这样
   ```

2. **清除浏览器缓存**:
   - Chrome: `Ctrl + Shift + Delete`
   - 或强制刷新: `Ctrl + F5`

3. **检查控制台错误**:
   - 按 `F12` 打开开发者工具
   - 查看 Console 标签是否有错误

---

## 📊 修复代码对比

### 修复前（script.js:332）:
```javascript
// ❌ 错误代码
sourceY = originalHeight - (index + 1) * lineHeight;
// 导致:
// - index=1: sourceY = 原图高度 - 2倍行高（从中间截取）
// - index=2: sourceY = 原图高度 - 3倍行高（从上方截取）
```

### 修复后（script.js:333）:
```javascript
// ✅ 正确代码
sourceY = originalHeight - lineHeight;
// 效果:
// - 所有index: sourceY = 原图高度 - 1倍行高（固定从底部截取）
```

---

## 🎉 预期效果示例

假设原图高度为 1000px，字幕行高为 50px：

### 修复前（错误）:
```
第1行: sourceY = 950px   (原图底部) ✅
第2行: sourceY = 900px   (原图中部) ❌
第3行: sourceY = 850px   (原图上方) ❌
```

### 修复后（正确）:
```
第1行: sourceY = 950px   (原图底部) ✅
第2行: sourceY = 950px   (原图底部) ✅
第3行: sourceY = 950px   (原图底部) ✅
```

---

**测试日期**: 2026-01-05
**修复版本**: v1.1
