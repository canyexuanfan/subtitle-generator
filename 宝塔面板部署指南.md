# 图片字幕生成器 - 宝塔面板部署完整指南

## 📌 部署前准备

### 项目分析
- **项目类型**: 纯前端静态网站应用
- **不需要**: Python运行环境、Node.js、数据库
- **需要**: Nginx/Apache静态文件服务器
- **server.py**: 仅用于本地开发,生产环境不使用

### 文件清单
需要上传到服务器的文件:
```
✅ index.html        # 主页面
✅ script.js         # JavaScript逻辑
✅ style.css         # 样式文件
✅ image.png         # 项目图片资源
❌ server.py         # 不需要上传(仅本地开发用)
❌ prd.md            # 不需要上传(文档文件)
❌ subtitle-image-*  # 生成的示例图片,可选上传
```

---

## 🛠️ 部署步骤详解

### 步骤一: 准备部署文件包

1. **创建部署文件夹**
   ```bash
   # 在本地创建一个干净的部署目录
   mkdir 图片字幕生成器-部署包
   ```

2. **复制必要文件**
   只复制以下文件到部署包:
   - `index.html`
   - `script.js`
   - `style.css`
   - `image.png` (如果页面中引用了)

3. **打包成ZIP**
   将上述文件打包成 `subtitle-generator.zip`

---

### 步骤二: 宝塔面板创建网站

#### 2.1 登录宝塔面板
```
访问: http://你的服务器IP:8888
输入宝塔管理员账号密码登录
```

#### 2.2 添加站点
1. 点击左侧菜单 **"网站"**
2. 点击 **"添加站点"**
3. 填写配置:
   ```
   域名: subtitle.你的域名.com  (或使用服务器IP)
   备注: 图片字幕生成器
   根目录: /www/wwwroot/subtitle-generator
   FTP: 不创建
   数据库: 不创建
   PHP版本: 纯静态 (或选择任意版本,不影响)
   ```
4. 点击 **"提交"**

#### 2.3 配置站点目录权限
1. 在网站列表中找到刚创建的站点
2. 点击 **"设置"**
3. 进入 **"网站目录"**
4. 确认:
   - 运行目录: `/` (根目录)
   - 防跨站攻击: 关闭 (静态站点不需要)

---

### 步骤三: 上传文件

#### 方法A: 使用宝塔面板在线上传
1. 点击左侧菜单 **"文件"**
2. 导航到 `/www/wwwroot/subtitle-generator/`
3. 点击 **"上传"**
4. 拖拽或选择 `subtitle-generator.zip`
5. 上传完成后,右键zip文件,选择 **"解压"**
6. 解压完成后,删除zip文件

#### 方法B: 使用FTP工具上传
1. 下载FTP工具(推荐: FileZilla)
2. 连接信息:
   ```
   主机: 你的服务器IP
   端口: 21
   用户名: 在宝塔"FTP"菜单中创建
   密码: 创建FTP时设置的密码
   ```
3. 上传所有文件到 `/www/wwwroot/subtitle-generator/`

---

### 步骤四: 配置Nginx (重要)

#### 4.1 添加安全头
1. 在网站列表中点击站点的 **"设置"**
2. 点击 **"配置文件"**
3. 在 `location /` 块中添加以下配置:

```nginx
server {
    listen 80;
    server_name subtitle.你的域名.com;

    root /www/wwwroot/subtitle-generator;
    index index.html;

    # 添加安全响应头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # 启用Gzip压缩
    gzip on;
    gzip_types text/plain text/css application/javascript image/svg+xml;

    # 静态资源缓存
    location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # 处理SPA路由(如果需要)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
    }
}
```

4. 点击 **"保存"**
5. 点击 **"重载配置"**

#### 4.2 配置HTTPS (强烈推荐)
1. 在网站设置中点击 **"SSL"**
2. 选择 **"Let's Encrypt"** (免费证书)
3. 勾选你的域名
4. 点击 **"申请"**
5. 等待证书申请成功
6. 开启 **"强制HTTPS"**

---

### 步骤五: 验证部署

#### 5.1 访问测试
```
HTTP访问: http://你的域名或IP
HTTPS访问: https://你的域名
```

#### 5.2 功能测试清单
- ✅ 页面能够正常加载
- ✅ 样式显示正确
- ✅ 可以上传图片
- ✅ 字幕设置功能正常
- ✅ 实时预览功能工作
- ✅ 可以生成并下载字幕图片

#### 5.3 性能检查
打开浏览器开发者工具(F12):
1. **Network标签**: 检查所有资源加载成功
2. **Console标签**: 确认无JavaScript错误
3. **Performance标签**: 页面加载时间应 < 2秒

---

## 🔒 安全加固建议

### 1. 添加Content Security Policy
在 `index.html` 的 `<head>` 中添加:
```html
<meta http-equiv="Content-Security-Policy"
      content="default-src 'self';
               script-src 'self' 'unsafe-inline';
               style-src 'self' 'unsafe-inline';
               img-src 'self' data: blob:;">
```

### 2. 服务器端文件大小限制
在宝塔面板中:
1. 点击 **"软件商店"** → **"Nginx"** → **"设置"**
2. 找到 `client_max_body_size`
3. 设置为 `10m` (限制上传文件最大10MB)

### 3. 配置防火墙
1. 点击左侧菜单 **"安全"**
2. 开启 **"SSH登录报警"**
3. 添加端口规则:
   - 开放: 80 (HTTP)
   - 开放: 443 (HTTPS)
   - 关闭: 其他不需要的端口

---

## 🎯 优化建议

### 性能优化
1. **启用HTTP/2**
   - 在网站SSL设置中开启HTTP/2

2. **CDN加速** (可选)
   - 使用阿里云CDN或腾讯云CDN
   - 加速静态资源加载

3. **图片懒加载**
   - 如果页面有多个图片,可添加懒加载

### 监控和维护
1. **访问日志分析**
   - 定期查看 `/www/wwwlogs/` 下的访问日志

2. **磁盘空间监控**
   - 用户上传的图片会占用临时空间(浏览器端处理)
   - 定期清理不需要的文件

3. **备份策略**
   - 在宝塔面板 **"计划任务"** 中
   - 设置每周备份网站文件和数据库(如果有)

---

## 🐛 常见问题排查

### 问题1: 页面404错误
**原因**: 文件路径不正确或未上传
**解决**:
```bash
# 检查文件是否存在
ls -la /www/wwwroot/subtitle-generator/
# 应该看到: index.html, script.js, style.css
```

### 问题2: 样式不生效
**原因**: CSS/JS文件路径错误
**解决**:
1. 打开浏览器开发者工具(F12)
2. 查看Network标签,找到404的资源
3. 确认 `index.html` 中的引用路径:
   ```html
   <link rel="stylesheet" href="style.css">   ✅ 正确
   <link rel="stylesheet" href="/style.css">  ❌ 可能错误
   ```

### 问题3: 上传图片功能不工作
**原因**: JavaScript权限或浏览器兼容性
**解决**:
1. 确认使用现代浏览器(Chrome 80+, Firefox 75+)
2. 检查浏览器Console是否有错误
3. 确认HTTPS已启用(某些API需要安全上下文)

### 问题4: 网站无法访问
**原因**: 防火墙或Nginx配置问题
**解决**:
```bash
# 检查Nginx状态
systemctl status nginx

# 重启Nginx
systemctl restart nginx

# 检查端口监听
netstat -tuln | grep :80
netstat -tuln | grep :443
```

---

## 📊 部署检查清单

部署完成后,请逐项检查:

- [ ] 所有必要文件已上传
- [ ] Nginx配置正确并已重载
- [ ] 域名DNS解析已配置
- [ ] SSL证书已申请并启用
- [ ] 安全响应头已添加
- [ ] 防火墙规则已配置
- [ ] 网站可以正常访问
- [ ] 所有功能测试通过
- [ ] 性能指标符合要求
- [ ] 已设置定期备份

---

## 🆘 技术支持

如果遇到部署问题:
1. 查看宝塔面板错误日志: `/www/wwwlogs/`
2. 查看Nginx错误日志: `tail -f /www/wwwlogs/nginx_error.log`
3. 检查服务器系统日志: `journalctl -xe`

---

**部署指南版本**: v1.0
**最后更新**: 2026-01-05
**适用宝塔版本**: 7.x 及以上
